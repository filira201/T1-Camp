{
  "name": "json-server-api",
  "version": "1.0.0",
  "scripts": {
    "startLoc": "json-server --watch db.json --port 3000",
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
